# hex6

The sixth generation of the Hex engine.


## Getting Started

Run a test DAG with:

```bash
uv run hex run core.main -w tests/integration/ws_simple/
```

Run the test suite with:

```bash
uv run pytest
```
